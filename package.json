{
  "name": "netlify-functions-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "netlify-cli": "^2.6.0",
    "netlify-lambda": "^1.1.1"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp src/index.html dist/ && cp -r data/ dist/data"
  }
}
